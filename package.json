{
  "name": "work-hours-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch backend/server.js",
    "start": "node backend/server.js",
    "check": "node --check src/main.js && node --check backend/server.js && node --test",
    "test": "node --test"
  },
  "description": "React dashboard with a Node.js and MongoDB backend for attendance analysis.",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongodb": "^7.6.0"
  }
}
